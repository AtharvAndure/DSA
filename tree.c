#include<stdio.h>
#include<stdlib.h>

typedef struct Tree{
    int data;
    struct Tree * right;
    struct Tree * left;   
}tree;
int main(){
    struct Tree * root=(tree *)malloc(sizeof(tree));
    root->data=50;
    root->left=NULL;
    root->right=NULL;
    struct tree * parent1=(tree *)malloc(sizeof(tree));
    return 
    // isefgiewgf
    // sudgfyesfgeuifg
    //  eiufaugfeugfeu